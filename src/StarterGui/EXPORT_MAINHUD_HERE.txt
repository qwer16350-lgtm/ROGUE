ROGUE — MainHUD 는 StarterGui 정본이 아님

Stage 전투 HUD 템플릿은 ReplicatedStorage.UIAssets.Stage 에 둡니다.

1) Studio에서 MainHUD(ScreenGui) 선택 → Save to File
2) 저장 경로(레포): src/ReplicatedStorage/UIAssets/Stage/MainHUD.rbxmx  (또는 .rbxm)
3) 루트 이름 MainHUD / ScreenGui 권장. 하위 구조는 HUDClient·LevelUpClient 문서와 맞출 것.

Rojo 동기화 후 StageClient 가 PlayerGui 로 Clone 한다.
StarterGui 폴더에는 MainHUD 를 넣지 말 것(중복 방지).
